#CHOICE
在采用二级页表的分页系统中，CPU页表基址寄存器中的内容是()
#OPTIONS
当前进程的一级页表的起始虚拟地址
当前进程的一级页表的起始物理地址
当前进程的二级页表的起始虚拟地址
当前进程的二级页表的起始物理地址
#CORRECT
2
#END

#CHOICE
在虚拟内存管理中，地址变换机构将逻辑地址变换为物理地址，形成该逻辑地址的阶段是( )。
#OPTIONS
编辑
编译
链接
装载
#CORRECT
3
#END

#CHOICE
下列关于页式存储管理的论述中，正确的是( )。
#OPTIONS
页式存储管理不会产生内部碎片
页式存储管理中的页面是为用户所能感知的
页式存储方式可以采用静态重定位
其余选项全部错误
#CORRECT
4
#END

#CHOICE
在页式存储管理中，当CPU形成一个有效地址时，查找页表的工作是由( )实现的。
#OPTIONS
操作系统
页表查询程序
硬件
存储管理进程
#CORRECT
3
#END

#CHOICE
操作系统采用基本分页存储管理方式，要求()。
#OPTIONS
每个进程拥有一张页表，且所有进程的页表大多数驻留在内存中
每个进程拥有一张页表，但只有执行进程的页表驻留在内存中
所有进程共享一张页表，但页表必须驻留在内存中
所有进程共享一张页表，且只有页表中当前使用的页面必须驻留在内存中
#CORRECT
1
#END

#CHOICE
某计算机采用二级页表的分页存储管理方式，按字节编址，页大小为 2^10 B，页表项大小为 2B，逻辑地址结构为
页目录号 | 页号 | 页内偏移量
逻辑地址空间大小为 2^16 页，则表示整个逻辑地址空间的页目录表中包含表项的个数至少是( )
#OPTIONS
64
128
256
512
#CORRECT
2
#END

#CHOICE
进程R和S共享数据 data，若 data 在R和S中所在页的页号分别为 p1 和 p2，两个页所对应的页框号分别为 f1 和 f2，则下列叙述中，正确的是()。
#OPTIONS
p1 和 p2 一定相等，f1 和 f2 一定相等
p1 和 p2 一定相等，f1 和 f2 不一定相等
p1 和 p2 不一定相等，f1 和 f2 一定相等
p1 和 p2 不一定相等，f1 和 f2 不一定相等
#CORRECT
3
#END

